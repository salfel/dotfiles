[module/github]
type = internal/github

token = ${file:~/.github/access.token}

user = salfel
empty-notifications = true

format =   <label>
format-foreground = ${colors.github}
format-underline = ${colors.github}
label = %notifications%

format-offline = <label-offline>
format-offline-foreground = ${colors.github}
format-offline-underline = ${colors.github}
label-offline = Offline
